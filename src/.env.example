# When USE_AWS_SECRETS=false, these are used directly
USE_AWS_SECRETS=false
DB_USER=app_user
DB_PASSWORD=app_password
DB_DSN=localhost:1521/FREEPDB1

# When USE_AWS_SECRETS=true, set AWS Secrets Manager values
AWS_REGION=us-east-1
AWS_SECRET_NAME=oracle/app/credentials
